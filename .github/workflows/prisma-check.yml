name: Prisma Bot
on: [push]
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - install: npm install
      - run: npx prisma validate # Tells you if your schema is broken before Vercel tries to build
